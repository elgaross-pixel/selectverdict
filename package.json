{
  "name": "selectverdict",
  "type": "module",
  "version": "0.0.1",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/generate-editorial-registry.mjs && node scripts/qa-scheduled-content.mjs",
    "build": "astro build && node scripts/qa-built-site.mjs",
    "qa": "node scripts/qa-scheduled-content.mjs && npm run build",
    "test:scheduler": "node --test tests/publish-scheduled.test.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": { "@astrojs/sitemap": "^3.7.3", "astro": "^7.2.6" },
  "allowScripts": { "esbuild": true }
}
