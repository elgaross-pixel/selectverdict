#!/usr/bin/env node
import { readdir, readFile } from 'node:fs/promises';import path from 'node:path';import { fileURLToPath } from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),dist=path.join(root,'dist'),origin='https://selectverdict.com';
async function walk(dir){const out=[];for(const e of await readdir(dir,{withFileTypes:true})){const f=path.join(dir,e.name);if(e.isDirectory())out.push(...await walk(f));else out.push(f)}return out}
function routeFor(file){const rel=path.relative(dist,file).replaceAll(path.sep,'/');if(rel==='index.html')return '/';if(rel.endsWith('/index.html'))return `/${rel.slice(0,-'/index.html'.length)}/`;return `/${rel.replace(/\.html$/,'')}`}
function normalize(href,base){try{const u=new URL(href,new URL(base,origin));if(u.origin!==origin)return null;return u.pathname==='/'?'/':`${u.pathname.replace(/\/$/,'')}/`}catch{return null}}
function canonical(html){return (html.match(/<link[^>]+rel=["']canonical["'][^>]+href=["']([^"']+)/i)||html.match(/<link[^>]+href=["']([^"']+)["'][^>]+rel=["']canonical["']/i))?.[1]||null}
const all=await walk(dist),pages=new Map();for(const file of all.filter(f=>f.endsWith('.html'))){const route=routeFor(file);pages.set(route,{file,html:await readFile(file,'utf8')})}
const sitemap=new Set();for(const file of all.filter(f=>/sitemap.*\.xml$/.test(path.basename(f)))){const xml=await readFile(file,'utf8');if(!/<urlset[\s>]/.test(xml))continue;for(const m of xml.matchAll(/<loc>(https:\/\/selectverdict\.com[^<]+)<\/loc>/g)){const r=normalize(m[1],'/');if(r)sitemap.add(r)}}
const incoming=new Map([...pages.keys()].map(r=>[r,0])),broken=[];for(const[route,{html}]of pages){for(const m of html.matchAll(/<a\b[^>]*href=["']([^"'#]+)["']/gi)){const raw=m[1];if(/pages\.dev|scheduled-content|src\/pages/.test(raw))broken.push(`${route} -> forbidden ${raw}`);const target=normalize(raw,route);if(!target)continue;if(pages.has(target))incoming.set(target,(incoming.get(target)||0)+1);else if(!/\.(svg|png|jpg|jpeg|webp|ico|xml|txt|css|js|pdf)\/$/i.test(target))broken.push(`${route} -> ${target}`)}}
const editorial=/^\/(reviews|pricing|guides|alternatives|compare|best)\/[^/]+\//,errors=[];const library=pages.get('/software/')?.html||'';for(const[route,{html}]of pages){if(!editorial.test(route))continue;const c=canonical(html);if(!c||normalize(c,route)!==route)errors.push(`${route}: wrong/missing canonical (${c||'none'})`);if(/<meta[^>]+name=["']robots["'][^>]+content=["'][^"']*noindex/i.test(html))errors.push(`${route}: accidental noindex`);if(!sitemap.has(route))errors.push(`${route}: missing from sitemap`);if((incoming.get(route)||0)===0)errors.push(`${route}: orphan page`);if(!route.startsWith('/reviews/')&&!library.includes(`href=\"${route}\"`)&&!library.includes(`href='${route}'`))errors.push(`${route}: missing from /software/ library`)}for(const route of sitemap)if(!pages.has(route))errors.push(`${route}: sitemap URL has no built HTML`);errors.push(...broken.map(x=>`broken internal link: ${x}`));
const commercialRoutes=['/reviews/se-ranking/','/reviews/mangools/','/compare/se-ranking-vs-mangools/','/guides/seo-tools-benchmark-1/'];
for(const route of commercialRoutes){const html=pages.get(route)?.html||'';if(!html.includes('data-sv-event="merchant_click"'))errors.push(`${route}: merchant-click instrumentation missing`);if(!html.includes(`data-sv-source-page="${route}"`))errors.push(`${route}: source-page attribution missing`);if(!html.includes('data-sv-merchant="se-ranking"')&&!route.includes('/reviews/mangools/'))errors.push(`${route}: SE Ranking merchant attribution missing`);if(!html.includes('data-sv-merchant="mangools"')&&!route.includes('/reviews/se-ranking/'))errors.push(`${route}: Mangools merchant attribution missing`)}
for(const route of ['/reviews/mangools/','/compare/se-ranking-vs-mangools/','/guides/seo-tools-benchmark-1/']){const html=pages.get(route)?.html||'';if(!/rel="[^"]*sponsored[^"]*nofollow[^"]*noopener[^"]*"[^>]*data-sv-merchant="mangools"/.test(html))errors.push(`${route}: compliant Mangools affiliate rel/attribution missing`);if(!html.includes('SelectVerdict may earn a commission'))errors.push(`${route}: affiliate disclosure missing`)}
const seReview=pages.get('/reviews/se-ranking/')?.html||'';if(!seReview.includes('data-sv-affiliate="false"'))errors.push('/reviews/se-ranking/: non-affiliate state missing');if(seReview.includes('data-sv-affiliate="true"'))errors.push('/reviews/se-ranking/: unconfirmed affiliate state present');
const shell=pages.get('/')?.html||'';if(!shell.includes('selectverdict:merchant-click')||!shell.includes('destinationOrigin'))errors.push('provider-neutral merchant event dispatcher missing');
if(errors.length){console.error(`Build QA FAIL (${errors.length})\n`+errors.map(e=>`- ${e}`).join('\n'));process.exit(1)}console.log(`Build QA PASS: ${pages.size} HTML routes; ${sitemap.size} sitemap routes; editorial library/sitemap/orphan/link/canonical/noindex checks passed.`);
