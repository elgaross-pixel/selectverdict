# Comparison data

Add one JSON or YAML file per researched comparison. A published comparison must reference at least two published product slugs and include sources, a verification date, methodology, and a transparent conclusion. Published references are resolved at build time; missing or draft products fail the build. `_template.json` is explicitly a draft placeholder and does not generate a route; no commercial comparison is published.
