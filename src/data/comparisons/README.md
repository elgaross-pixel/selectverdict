# Comparison data

Add one JSON or YAML file per researched comparison. A published comparison must reference at least two published product slugs and include sources, a verification date, and a transparent conclusion. `_template.json` is explicitly a draft placeholder and does not generate a route; no commercial comparison is published.
